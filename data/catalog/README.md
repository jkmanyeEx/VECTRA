# Data catalog

The catalog contains lightweight, non-sensitive indexes of available runs.
Catalog rows may reference run IDs, dates, geometry IDs, environmental
conditions, processing versions, and quality status, but not private device
identifiers or raw telemetry.
