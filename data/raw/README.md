# Raw data

Raw run directories are immutable evidence. A run directory contains the
original PX4 ULog, external sensor logs, operator notes, and the exact
`resolved_config.json` generated at run start.

Raw recordings are ignored by Git. Never overwrite or normalize files here.
